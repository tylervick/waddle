# Ledger triage record

Extraction of `.superpowers/sdd/progress.md` into GitHub issues, per
`docs/superpowers/specs/2026-08-06-agent-substrate-design.md`. That ledger is
gitignored and exists on one machine; this file is the durable account of what
was in it and what happened to each item.

Every candidate was checked against current code before classification.
"Unreproducible" means the described condition could not be found at HEAD — the
spec prefers recording that over filing on suspicion.

| Candidate | Origin | Disposition | Note |
| --- | --- | --- | --- |
| `libSDL3_test.a` installed despite `SDL_TESTS=OFF` | P1 T4 | already fixed | `Scripts/build-deps.sh` passes `-DSDL_TEST_LIBRARY=OFF` (commit 500f1a9), which SDL's CMake needs independently of `SDL_TESTS`; commit message records a clean-rebuild verification that the lib is no longer built or installed. |
| `woof_ios.c` cross-thread `I_Error` longjmp UB (theoretical) | P1 T5 | already fixed | Commit 500f1a9 added a `pthread_equal` check in `WoofIOS_ExitUnwind`: a call from any thread other than the session thread now `abort()`s instead of longjmp-ing across stacks. |
| `i_system.c` `errmsg` `strcat` accumulates across sessions | P1 T5 | already fixed | Commit 500f1a9 added `I_ResetErrorMessages()` (i_system.c), called at the top of every `WoofIOS_Run` session before `D_DoomMain`. |
| `src/CMakeLists.txt` one-char whitespace drift from pristine | P1 T5 | already fixed | Commit 500f1a9 restored the trailing space after `RUNTIME_DEPENDENCIES` that a prior edit had dropped. |
| `fetch-freedoom.sh` dead `-z EXPECTED_HASH` branch under `pipefail` | P1 T7 | already fixed | Commit 500f1a9 added `\|\| true` to the `grep \| awk` pipeline so a failed match reaches the `-z` check instead of killing the script via `set -e -o pipefail`. |
| `COPYING*` find last-write-wins | P1 T7 | already fixed | Commit 500f1a9 picks the license file deterministically (`find ... \| sort \| sed -n '1p'`) instead of letting the last `-exec cp` win. |
| `memset` `sa_mask` instead of `sigemptyset` | P1 T10 | already fixed | Commit 3eea32a (Plan 3 Task 1) added an explicit `sigemptyset(&ignore_term.sa_mask);` alongside the existing `memset`, tightening the SIGTERM setup — done incidentally while adding the touch-control shim, never struck off Plan 1's ledger. |
| Unreachable `return 0` path skips SIGTERM restore | P1 T10 | issue #37 | Still present: `WoofIOS_Run`'s `return 0;` right after `D_DoomMain()` does not restore `previous_sigterm`; only the setjmp-unwind branch does. Defensive-only (`D_DoomMain` never returns today). |
| `AddWadInMem` data buffer leak (unreachable) | P1 T10 | issue #38 | Still present: `Engine/woof/src/w_zip.c`'s `AddWadInMem` never frees its `malloc`'d `data` buffer before either `I_Error` call on the two error paths. |
| `M_LoadDefaults` `strdup` leak (pre-existing) | P1 T10 | issue #39 | Still present: `m_config.c:715` overwrites `*dp->location.s` with a fresh `strdup` every call with no prior `free`; `M_InitConfig`'s own comment confirms `M_LoadDefaults` runs every iOS session, turning a harmless-upstream pattern into a per-session leak. |
| Autoquit timer session-scoping | P1 final review | already fixed | `EngineSession.swift`'s `sessionGeneration` counter + `isCurrentGeneration(_:)` guard the autoquit `Thread.detachNewThread` callback; `App/Tests/EngineSessionGenerationTests.swift` directly tests it with a doc comment citing this exact ledger item. |
| `I_AtSignal` per-session growth | P1 final review | already fixed | `Engine/woof/src/i_exit.c`'s `I_AtSignal` now skips (under `WOOF_IOS`) a `func` already present in `atsignal_funcs` instead of re-pushing it every session, with a comment explaining why (nothing drains this list short of a fatal signal). |
| `ZipExtractor` per-entry size cap | P2 T4 | already fixed | `App/Sources/WAD/ZipExtractor.swift:15,42` implements `maxEntryBytes`/the `uncompressedSize` guard; `App/Tests/ZipExtractorTests.swift`'s `testOversizeEntriesAreSkippedAndReported` proves both the reject and the still-extracts-a-normal-entry paths. Implemented during Plan 3 Task 7 per the ledger's own hint. |
| No direct `sha1(of:)` / `.unreadable` tests | P2 T3 | already fixed | `App/Tests/WADStoreTests.swift`'s `testStreamedHashMatchesInMemoryHash` and `testUnreadableSourceThrows` test exactly these directly. |
| `allLoadouts` sort ordering has no direct test | P2 T5 | already fixed | `App/Tests/LibraryServiceTests.swift`'s `testAllLoadoutsSortsMostRecentFirst` asserts the most-recent-first order directly. |
| Bundled pseudo-hash means user-imported Freedoom copies do not dedupe | P2 T5 | issue #40 | Still present: `LibraryService.swift:47` stores `sha1: "bundled:\(entry.file)"` for seeded bundled IWADs. The per-launch-hashing-cost rationale in the Plan 2 design doc doesn't block a fix, since the row (and therefore any hashing) is only ever created once, guarded by an existence check. |
| `adoptLooseFiles` reject-check is post-hoc by basename | P2 T6 | already fixed | `ImportService.swift`'s `adoptLooseFiles` now gives each candidate its own local `ImportOutcome` instead of diffing a shared dictionary's keys; `App/Tests/ImportServiceTests.swift`'s `testAdoptLooseFilesQuarantinesBothZipsWhenOversizeEntriesShareABasename` is a named regression test for exactly this collision. |
| Two `LoadoutArguments` tests leave `Saves/<uuid>` litter | P2 T7 | already fixed | `testIWADOnly` and `testFullStackKeepsPWADOrderAndSpaces` in `App/Tests/LoadoutArgumentsTests.swift` both now carry an `addTeardownBlock` that removes `LibraryService.savesDirectory(forLoadoutID:)`. (Two newer tests added since, `testBuildBaseGameOnlyArgv`/`testBuildWithPWADsAndComplevel`, create a saves directory via the same code path and don't clean up either, but that's a new instance outside this candidate's original scope, not filed here.) |
| Preset-editor create-error swallowed by `try?` | P2 T8 | issue #41 | Still present: `LoadoutEditorView.swift`'s `save()` does `let loadout = try? library.createLoadout(...)` then unconditionally calls `complete()`, dismissing the editor even when creation failed. |
| `LibraryView` multi-delete last-blocked-list only (dead code today) | P2 T8 | issue #42 | Still present: `LibraryView.swift`'s `delete(_:)` does `deleteBlocked = names` (overwrite, not accumulate) inside a loop over possibly-multiple `offsets`. Still dead code today (no multi-select delete UI exists), same as the ledger noted. |
| `fatalError` on container init | P2 T8 | won't fix | `WADdleApp.swift:40` still `fatalError`s on `ModelContainer` init failure. `docs/app-store/submission-checklist.md`'s "Known limitations (for the record, no action needed)" section explicitly lists this ("container-init recovery needs design; risk is cold-start-only") — a human-reviewed, documented, accepted trade-off, not an oversight. |
| `testWrongIWADPairingFailsSoft` name no longer matches its scenario | P2 T9 | already fixed | Test was renamed to `testUnrecognizedIWADFailsSoft` in commit 225385e (Plan 3 Task 8, "test hygiene"). |
| Mixed-result zips delete rejected members | P2 final review | already fixed | `ImportService.swift`'s `adoptLooseFiles` quarantines (moves to `Documents/Import Failed/`) any candidate that contributed at least one rejection, whether or not it also contributed an import — it no longer deletes a partially-successful zip. `testAdoptLooseFilesQuarantinesBothZipsWhenOversizeEntriesShareABasename` explicitly covers a zip that both imports a valid file and gets quarantined. `docs/app-store/submission-checklist.md` additionally records this as "documented behavior, not a bug." |
| Share-sheet import gives no UI feedback / no `LibraryView` refresh | P2 final review | already fixed | `WADdleApp.swift`'s `onOpenURL` now posts to `ImportNotices.shared` and `NotificationCenter.default.post(name: .libraryDidChange, ...)`; `LibraryView` observes `.libraryDidChange` and calls `refresh()`. |
| `guard` vs `precondition` on `EngineSession.play` | P2 final review | already fixed | Deliberate, documented split (Plan 4 Task 3 "guard-not-precondition" step): the `!isRunning` reentrancy check is a `guard` returning a soft error code; the `arguments.first == "woof"` internal-contract check intentionally remains a `precondition`, kept on purpose per the Plan 4 plan doc. |
