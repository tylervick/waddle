# Ledger triage record

Extraction of `.superpowers/sdd/progress.md` into GitHub issues, per
`docs/superpowers/specs/2026-08-06-agent-substrate-design.md`. That ledger is
gitignored and exists on one machine; this file is the durable account of what
was in it and what happened to each item.

Every candidate was checked against current code before classification.
"Unreproducible" means the described condition could not be found at HEAD — the
spec prefers recording that over filing on suspicion.

| Candidate | Origin | Disposition | Note |
| --- | --- | --- | --- |
| `libSDL3_test.a` installed despite `SDL_TESTS=OFF` | P1 T4 | already fixed | `Scripts/build-deps.sh` passes `-DSDL_TEST_LIBRARY=OFF` (commit 500f1a9), which SDL's CMake needs independently of `SDL_TESTS`; commit message records a clean-rebuild verification that the lib is no longer built or installed. |
| `woof_ios.c` cross-thread `I_Error` longjmp UB (theoretical) | P1 T5 | already fixed | Commit 500f1a9 added a `pthread_equal` check in `WoofIOS_ExitUnwind`: a call from any thread other than the session thread now `abort()`s instead of longjmp-ing across stacks. |
| `i_system.c` `errmsg` `strcat` accumulates across sessions | P1 T5 | already fixed | Commit 500f1a9 added `I_ResetErrorMessages()` (i_system.c), called at the top of every `WoofIOS_Run` session before `D_DoomMain`. |
| `src/CMakeLists.txt` one-char whitespace drift from pristine | P1 T5 | already fixed | Commit 500f1a9 restored the trailing space after `RUNTIME_DEPENDENCIES` that a prior edit had dropped. |
| `fetch-freedoom.sh` dead `-z EXPECTED_HASH` branch under `pipefail` | P1 T7 | already fixed | Commit 500f1a9 added `\|\| true` to the `grep \| awk` pipeline so a failed match reaches the `-z` check instead of killing the script via `set -e -o pipefail`. |
| `COPYING*` find last-write-wins | P1 T7 | already fixed | Commit 500f1a9 picks the license file deterministically (`find ... \| sort \| sed -n '1p'`) instead of letting the last `-exec cp` win. |
| `memset` `sa_mask` instead of `sigemptyset` | P1 T10 | already fixed | Commit 3eea32a (Plan 3 Task 1) added an explicit `sigemptyset(&ignore_term.sa_mask);` alongside the existing `memset`, tightening the SIGTERM setup — done incidentally while adding the touch-control shim, never struck off Plan 1's ledger. |
| Unreachable `return 0` path skips SIGTERM restore | P1 T10 | issue #37 | Still present: `WoofIOS_Run`'s `return 0;` right after `D_DoomMain()` does not restore `previous_sigterm`; only the setjmp-unwind branch does. Defensive-only (`D_DoomMain` never returns today). |
| `AddWadInMem` data buffer leak (unreachable) | P1 T10 | issue #38 | Still present: `Engine/woof/src/w_zip.c`'s `AddWadInMem` never frees its `malloc`'d `data` buffer before either `I_Error` call on the two error paths. |
| `M_LoadDefaults` `strdup` leak (pre-existing) | P1 T10 | issue #39 | Still present: `m_config.c:715` overwrites `*dp->location.s` with a fresh `strdup` every call with no prior `free`; `M_InitConfig`'s own comment confirms `M_LoadDefaults` runs every iOS session, turning a harmless-upstream pattern into a per-session leak. |
| Autoquit timer session-scoping | P1 final review | already fixed | `EngineSession.swift`'s `sessionGeneration` counter + `isCurrentGeneration(_:)` guard the autoquit `Thread.detachNewThread` callback; `App/Tests/EngineSessionGenerationTests.swift` directly tests it with a doc comment citing this exact ledger item. |
| `I_AtSignal` per-session growth | P1 final review | already fixed | `Engine/woof/src/i_exit.c`'s `I_AtSignal` now skips (under `WOOF_IOS`) a `func` already present in `atsignal_funcs` instead of re-pushing it every session, with a comment explaining why (nothing drains this list short of a fatal signal). |
| `ZipExtractor` per-entry size cap | P2 T4 | already fixed | `App/Sources/WAD/ZipExtractor.swift:15,42` implements `maxEntryBytes`/the `uncompressedSize` guard; `App/Tests/ZipExtractorTests.swift`'s `testOversizeEntriesAreSkippedAndReported` proves both the reject and the still-extracts-a-normal-entry paths. Implemented during Plan 3 Task 7 per the ledger's own hint. |
| No direct `sha1(of:)` / `.unreadable` tests | P2 T3 | already fixed | `App/Tests/WADStoreTests.swift`'s `testStreamedHashMatchesInMemoryHash` and `testUnreadableSourceThrows` test exactly these directly. |
| `allLoadouts` sort ordering has no direct test | P2 T5 | already fixed | `App/Tests/LibraryServiceTests.swift`'s `testAllLoadoutsSortsMostRecentFirst` asserts the most-recent-first order directly. |
| Bundled pseudo-hash means user-imported Freedoom copies do not dedupe | P2 T5 | issue #40 | Still present: `LibraryService.swift:47` stores `sha1: "bundled:\(entry.file)"` for seeded bundled IWADs. The per-launch-hashing-cost rationale in the Plan 2 design doc doesn't block a fix, since the row (and therefore any hashing) is only ever created once, guarded by an existence check. |
| `adoptLooseFiles` reject-check is post-hoc by basename | P2 T6 | already fixed | `ImportService.swift`'s `adoptLooseFiles` now gives each candidate its own local `ImportOutcome` instead of diffing a shared dictionary's keys; `App/Tests/ImportServiceTests.swift`'s `testAdoptLooseFilesQuarantinesBothZipsWhenOversizeEntriesShareABasename` is a named regression test for exactly this collision. |
| Two `LoadoutArguments` tests leave `Saves/<uuid>` litter | P2 T7 | issue #43 | The two originally-named tests, `testIWADOnly` and `testFullStackKeepsPWADOrderAndSpaces` in `App/Tests/LoadoutArgumentsTests.swift`, are fixed (`addTeardownBlock` removes `LibraryService.savesDirectory(forLoadoutID:)`). But the same condition the ledger named — a `LoadoutArguments.build` call that creates a saves directory with no cleanup — is still present at HEAD in two newer tests, `testBuildBaseGameOnlyArgv` (:65) and `testBuildWithPWADsAndComplevel` (:75), so the candidate is not actually closed. |
| Preset-editor create-error swallowed by `try?` | P2 T8 | issue #41 | Still present: `LoadoutEditorView.swift`'s `save()` does `let loadout = try? library.createLoadout(...)` then unconditionally calls `complete()`, dismissing the editor even when creation failed. |
| `LibraryView` multi-delete last-blocked-list only (dead code today) | P2 T8 | issue #42 | Still present: `LibraryView.swift`'s `delete(_:)` does `deleteBlocked = names` (overwrite, not accumulate) inside a loop over possibly-multiple `offsets`. Still dead code today (no multi-select delete UI exists), same as the ledger noted. |
| `fatalError` on container init | P2 T8 | won't fix | `WADdleApp.swift:40` still `fatalError`s on `ModelContainer` init failure. `docs/app-store/submission-checklist.md`'s "Known limitations (for the record, no action needed)" section explicitly lists this ("container-init recovery needs design; risk is cold-start-only") — a human-reviewed, documented, accepted trade-off, not an oversight. |
| `testWrongIWADPairingFailsSoft` name no longer matches its scenario | P2 T9 | already fixed | Test was renamed to `testUnrecognizedIWADFailsSoft` in commit 225385e (Plan 3 Task 8, "test hygiene"). |
| Mixed-result zips delete rejected members | P2 final review | already fixed | `ImportService.swift`'s `adoptLooseFiles` quarantines (moves to `Documents/Import Failed/`) any candidate that contributed at least one rejection, whether or not it also contributed an import — it no longer deletes a partially-successful zip. `testAdoptLooseFilesQuarantinesBothZipsWhenOversizeEntriesShareABasename` explicitly covers a zip that both imports a valid file and gets quarantined. `docs/app-store/submission-checklist.md` additionally records this as "documented behavior, not a bug." |
| Share-sheet import gives no UI feedback / no `LibraryView` refresh | P2 final review | already fixed | `WADdleApp.swift`'s `onOpenURL` now posts to `ImportNotices.shared` and `NotificationCenter.default.post(name: .libraryDidChange, ...)`; `LibraryView` observes `.libraryDidChange` and calls `refresh()`. |
| `guard` vs `precondition` on `EngineSession.play` | P2 final review | already fixed | Deliberate, documented split (Plan 4 Task 3 "guard-not-precondition" step): the `!isRunning` reentrancy check is a `guard` returning a soft error code; the `arguments.first == "woof"` internal-contract check intentionally remains a `precondition`, kept on purpose per the Plan 4 plan doc. |
| `beginSessionForTesting` not `#if DEBUG` gated | P3 T3 | already fixed | `EngineSession.swift:35-45` wraps both `beginSessionForTesting()` and `setRunningForTesting(_:)` in `#if DEBUG`, added in commit 45d53f0 ("feat: privacy manifest and release hardening of debug seams"), which also gated every other `WADDLE_*` test seam the same way. |
| `weaponPrevButton` sits inside the stick region; a near-miss starts a stick track | P3 T4 | issue #44 | Still present: `TouchOverlayView.swift`'s `touchesBegan` (line 356) claims any touch with `point.x < bounds.width * 0.4` (line 369) as a stick touch with no exclusion for button-occupied space, and `weaponPrevButton` is placed at `x: b.minX + inset.left + 40` (line 344) — inside that same column. None of the post-PR rework commits touched this. |
| `OverlayButton` square hit area versus circular visual | P3 T4 | issue #45 | Still present: `OverlayButton` (`TouchOverlayView.swift:459`) gives its layer `cornerRadius = size / 2` (line 470) for a circular look but never overrides `point(inside:with:)`, so UIKit's default rectangular hit-testing accepts the whole square frame. |
| Unused-code warning at `ContentView:46` | P3 T6 hygiene | already fixed | At the time of the P3 Task 6 note, `ContentView.swift` bound an unused shadowing `let code = lastExitCode` inside the debug-counts overlay block. At HEAD that condition reads `lastExitCode != nil` instead (no unused binding), and the whole block is now also `#if DEBUG`-gated (commit 45d53f0). |
| `FORCE_TOUCH_OVERLAY` needs a one-line "test-only" note | P3 T6 hygiene | already fixed | `OverlayPresenter.swift`'s `applyPolicy()` carries a multi-line comment explaining the escape hatch is "Only ever set by the UI test; never present in a real session," and `docs/manual-testing.md:5` has the one-line note directly: "`WADDLE_FORCE_TOUCH_OVERLAY` is a test-only environment variable that bypasses the automatic overlay hide policy...". |
| `Task.detached` not under `.task` cancellation | P3 T7 | issue #46 | Still present: `ImportService.swift`'s `importOneAsync` (lines 295, 312) and `copyIntoStore` (line 377) all use `Task.detached`, an unstructured task with no inherited cancellation from `WADdleApp.swift:47`'s `.task { await runAdoption() }`; none of the three sites check `Task.isCancelled`. |
| No end-to-end oversize-only-zip `ImportService` test | P3 T7 | already fixed | `ImportServiceTests.swift:86`'s `testOversizeOnlyZipStillRejectedWithoutImporting` calls `importer.importFiles(at:)` (the actual `ImportService` entry point, not just `ZipExtractor` directly) with a zip whose only entry exceeds the cap, and asserts nothing imports. Added in commit 85d7375 (Plan 3 final review). |
| `UInt64(maxEntryBytes)` traps if a negative value is injected | P3 T7 | already fixed | `ZipExtractor.swift:26` now has `precondition(maxEntryBytes >= 0, "cap must be non-negative")` right before the `UInt64(maxEntryBytes)` conversion, with a comment explaining it turns an obscure conversion trap into a clear, immediate one. Added in commit 45d53f0. |
| `touch_event_count` not reset across sessions | P3 T1 forward note | issue #47 | Still present: `grep -n "touch_event_count" Engine/woof/src/woof_ios.c` shows it incremented in four places but never reset, including the per-session reset block (lines 157-164) that already resets `touch_joystick`/`touch_gamepad`/`touch_turn_accum` for the next session. |
| Hardcoded "512 MB" string | P3 final review | already fixed | `ImportService.swift`'s `oversizeRejectionReason` (line 60-62) now derives the user-facing number from `maxZipEntryBytes` (`"Entry exceeds the \(maxZipEntryBytes / (1024 * 1024)) MB import limit."`) instead of a literal string, with a comment noting it's "so the user-facing number can never drift from the enforced limit." Added in commit 8c569ea, folded into the 85d7375 final-review merge. |
| Per-axis/button debug getters so tests can assert *which* control was driven | P3 post-PR | issue #48 | Still present: `Engine/woof/src/woof_ios.c` exposes only `WoofIOS_DebugTouchEventCount()` (one cumulative counter) and `WoofIOS_DebugTriggerValue()` (fire trigger only); `TouchControlsTests.swift`'s tests work around the gap by isolating one gesture per test run instead of asserting on a differentiated per-control signal. |
| `TouchButton.east` dead code | P3 post-PR | issue #49 | Still present: a repo-wide search for `.east` as a `TouchButton` value matches only its own declaration (`TouchGamepad.swift:34`). The post-PR fix-round report noted it as "documented as intentionally unused... worth revisiting if a future scheme wants to bind something there," but it was never removed or bound. |
| `TouchOverlayView` dead default parameter | P3 post-PR | issue #50 | Still present: `TouchOverlayView.swift:34-35`'s initializer declares defaults for `scheme`, `tuning`, and `debugHUDEnabled`, but its sole call site (`OverlayPresenter.swift:79-80`) always supplies all three explicitly, and no test constructs a `TouchOverlayView` directly. |
| Deadzone stacking + turn feel (on-device tuning) | P3 final review / post-PR | issue #51 | Requires a physical device to judge: `TouchTuning`'s turnSpeed/stickDeadZone/moveSensitivity sliders exist and persist, but choosing correct defaults — and whether the current defaults feel right at all — needs real touch latency and hand size, which the simulator cannot reproduce. `agent:blocked`, not filed as eligible work. |
| On-device checklist must cover physical-input auto-hide | P3 T6 hygiene | already fixed | `docs/manual-testing.md` already has this covered: line 33 ("Overlay hides/shows when a controller connects/disconnects mid-session"), line 102 ("Overlay auto-hides when controller is connected and visible when disconnected... requires real hardware"), and line 107 ("overlay hidden while keyboard is connected"). |
| `suggestedIWAD` doc comment omits the most-recent tie-break | P4 T3 | unreproducible | `LibraryService.suggestedIWAD(for:)` — the function the doc comment lived on — no longer exists. Commit `d4870ca` ("refactor(play): guard base-game reconcile to run once; drop dead suggestedIWAD") removed it along with `App/Tests/SuggestedIWADTests.swift` entirely; a repo-wide search for `suggestedIWAD`/`SuggestedIWAD` at HEAD matches nothing under `App/Sources` or `App/Tests`. There is no doc comment left to fix. |
| `metadata.md:190` stale "landscape-only" phrase | P4 T7b | already fixed | Commit `46c60d3` ("fix: final-review polish...") removed the stale `(the app is landscape-only)` parenthetical from the iPhone device-class line, before Task 7b's own iPad-windowing fix even landed. `docs/app-store/metadata.md` now additionally frames the old landscape-only state as an explicit "Historical note, since it no longer applies" paragraph documenting *why* the capture script once needed a Settings workaround — correctly contextualized as history, not asserted as current. |
| Overlay fixed-pixel layout unverified below ~573×545 iPad windows, no min-size guard | P4 T7b | issue #52 | Still present: `TouchOverlayView.layoutSubviews()` (`App/Sources/Touch/TouchOverlayView.swift:325-348`) places every button at fixed pixel offsets from `bounds`/`safeAreaInsets` with no floor on how small `bounds` can get, and a repo-wide search for `sizeRestrictions`/`minimumSize` finds no `UIWindowScene` size floor configured anywhere in `App/Sources` or `App/project.yml` to keep iPadOS 26 windowed multitasking from producing a window that small in the first place. |
| Mixed-zip rejected-member edge | P4 final review | already fixed | Same underlying condition as this table's own "Mixed-result zips delete rejected members" row (P2 final review, above): `ImportService.adoptLooseFiles()` (`App/Sources/Library/ImportService.swift:98-140`) still gives each candidate a local `ImportOutcome` and quarantines (rather than deletes) any candidate that contributed at least one rejection, via `moveToImportFailed`; `App/Tests/ImportServiceTests.swift`'s `testAdoptLooseFilesQuarantinesBothZipsWhenOversizeEntriesShareABasename` and `testOversizeOnlyZipStillRejectedWithoutImporting` both still pass at HEAD. The Plan 4 final review's carry list re-mentioned this item without re-checking that it had already landed during Plan 3 — recorded here again only because the brief's Plan 4 candidate list names it separately by its own origin tag. |
| Preset-editor `try?` swallow | P4 final review | duplicate of issue #41 | Same code, same condition: `LoadoutEditorView.save()` (`App/Sources/UI/LoadoutEditorView.swift:140-156`) still does `let loadout = try? library.createLoadout(...)` and unconditionally calls `complete()` regardless of whether `loadout` came back `nil`. Issue #41 (filed in Task 6, sourced from Plan 2 Task 8) already documents this exact lineage in its own Provenance section: "carried unresolved through Plan 4's final review ('loadout-editor try? swallow')." No second issue filed. |
| Container-init recovery | P4 final review | won't fix | Same condition as this table's own "`fatalError` on container init" row (P2 T8, above): `WADdleApp.swift:40` still `fatalError`s on `ModelContainer` init failure. `docs/app-store/submission-checklist.md`'s "Known limitations (for the record, no action needed)" section still explicitly lists it ("container-init recovery needs design; risk is cold-start-only") — a standing, human-reviewed, documented accepted trade-off, not an oversight, and the checklist entry itself cross-references "the Plan 4 review notes." |
| Case-sensitive IWAD hint branch, cosmetic | P4 final review | issue #53 | Still present: `EngineErrorAlert.hint(for:)`'s inner branch (`App/Sources/UI/EngineErrorAlert.swift:24`, `if message.contains("IWAD")`) is a case-sensitive substring check, unchanged since it was introduced in commit `706201f` and untouched by commit `8c569ea`'s later, unrelated edit to the same function. For the `"Failed to load %s"` marker (`D_AddFile`, `Engine/woof/src/d_main.c:536,1248`), which of the two hint strings gets shown ends up depending only on incidental capitalization of the user's own filename rather than on the failure itself. |

**Totals (47 rows):** 26 already fixed, 17 filed as new issues (#37–#53), 2
won't fix, 1 unreproducible, 1 duplicate (of issue #41).

## Pre-existing issues brought into the taxonomy

Four issues predate this triage effort and were not part of the ledger
extraction above, but were folded into the same `agent:eligible`/
`agent:blocked` taxonomy so the label set means the same thing everywhere:

- **#13** (`build-deps.sh` tag-bump re-fetch bug), **#14** (scheduled
  cold-cache CI build), and **#15** (README CI documentation) all gained the
  `agent:eligible` label plus the three required headings — `## Definition of
  done`, `## Verification`, `## Provenance` — added to their bodies to match
  the format every newly-filed issue above already follows.
- **#28** (App Store Connect submission tracker) gained the `agent:blocked`
  label instead, per the Global Constraints list: it is owner-side App Store
  Connect data entry, one of the categories never eligible for unattended
  agent work.
