---
run_id: 2026-08-14T200101Z-issue-71
timestamp: 2026-08-14T20:01:01Z
prompt_sha: e7cb40d
issue: 71
kind: bug
size: size:s
outcome: stuck
wall_clock_seconds: 150
verification_result: not-run
ci_result: not-run
coderabbit_findings_first: none
coderabbit_findings_after: none
test_proof_first: error
test_proof_domains: none
fix_rounds: 0
pr: none
learning_added: none
---

Refused as owner-only work: the bug is real and fully reproduced, but every
file the fix touches is on `Scripts/loop-prompt.md`'s absolute never-modify
list. The definition of done names `Scripts/loop-report.sh`; the verification
section requires new cases in `Scripts/test-loop-report.sh`; and the provenance
section asks for `docs/learnings/masked-exit-status-fails-open.md` to be
updated, which is also forbidden (a run may add a learnings file, never rewrite
one). There is no lawful subset to land, so nothing was changed and no pull
request was opened. This is the same class as issues #68 and #14 — see
`docs/learnings/` and the stuck pile.

**The bug is confirmed at HEAD, on all three counts the issue claims.**

1. The code path. `Scripts/loop-report.sh:354-378` gates the live comments
   query on `is_reconciled`, which is set purely from `fix_rounds: 0` plus
   all-agent commit authorship. Nothing proves a review ever happened. The
   comment block at lines 355-364 states the intent outright — "an
   empty-but-successful result here is a real, valid measurement (a PR
   CodeRabbit reviewed and found nothing Major/Critical in -- PRs #57 and #59's
   actual shape)" — and that parenthetical is the factually wrong premise, not
   just an imprecise comment. It should be corrected in the same change.

2. The data. All three PRs have **zero reviews of any kind**, not merely zero
   trusted-app reviews, and zero trusted-app review comments:

   ```
   PR #57: trusted-app reviews=0  all reviews=0  trusted-app review comments=0
   PR #59: trusted-app reviews=0  all reviews=0  trusted-app review comments=0
   PR #70: trusted-app reviews=0  all reviews=0  trusted-app review comments=0
   ```

   Their records are `2026-08-08T012411Z-issue-13.md` (`none`),
   `2026-08-08T025710Z-issue-15.md` (`unavailable`), and
   `2026-08-11T030941Z-issue-43.md` (`unavailable`) — each `fix_rounds: 0` with
   a PR, so each is reconciliation-eligible and each reconciles at a fabricated
   0.

3. The report. `Scripts/loop-report.sh /tmp/lt/docs/loop-trials` today claims
   **14 reconciled records** across all prompt versions, so the blast radius is
   wider than the three PRs the issue names — the owner should expect more than
   three records to revert to `unavailable`, and should re-count after fixing
   rather than trusting the issue's figure of three.

**Baseline for whoever picks this up**, all green at HEAD before any change:
`Scripts/test-loop-report.sh` (all cases pass), `Scripts/test-loop-precheck.sh`
(pass), `Scripts/check-substrate.sh` (exit 0).

**Two things that will bite the fix, found while reproducing.** Neither is
mentioned in the issue.

- Existing case 23 (`test-loop-report.sh:695-724`) currently *asserts* the
  flawed behaviour: a bare `make_recon_fixture` with an emptied
  `review-body.txt` must reconcile at 0. Under the fix that fixture has no
  review, so it must stop reconciling — but the case must not be deleted or
  weakened, because it is exactly the "reviewed with no findings still
  reconciles at 0" half of the distinction the issue insists on preserving. The
  right move is to extend the fixture so the case keeps its meaning: give it a
  review, and let the new never-reviewed case be the one that refuses.
- `make_recon_fixture`'s stub `gh` (line 57) has arms only for
  `pulls/*/commits` and `pulls/*/comments`; everything else falls to a catch-all
  that prints `{"state":"MERGED","reviewDecision":""}`. A new
  `pulls/<pr>/reviews` query would hit that catch-all and be parsed as if it
  were review data — a silently wrong pass. The stub needs its own `reviews`
  arm plus `review-list.txt` / `review-exit.txt` control files, mirroring the
  existing pair, so the new tests can drive the reviewed and never-reviewed
  cases independently and simulate a failed reviews query.

One judgement call worth recording for the owner: the reviews query needs the
same exit-status-first discipline as the commits query, and its empty verdict
must fall the *same* way — an empty review list means "no review", which is
precisely the case being ruled out, so unlike the comments query there is no
tension between "failed" and "empty" here. Both must land on `unavailable`.
