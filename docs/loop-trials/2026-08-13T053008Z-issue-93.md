---
run_id: 2026-08-13T053008Z-issue-93
timestamp: 2026-08-13T05:30:08Z
prompt_sha: 362768d
issue: 93
kind: enhancement
size: m
outcome: pr-opened
wall_clock_seconds: 1907
verification_result: pass
ci_result: pass
coderabbit_findings_first: unavailable
coderabbit_findings_after: none
test_proof_first: proved
test_proof_domains: shell
fix_rounds: 0
pr: 101
learning_added: none
---

Clean trial: reproduce, redesign, prove, PR #101 opened, CI green in 6m00s,
`TEST_PROOF: proved` (shell) from the first run.

The issue carried a contradiction worth noting: its provenance section says
"blocked for the loop", but its live labels were `agent:eligible` +
`agent:next` — the owner's later, deliberate decision (the same day PR #97
split the release path and made the notes assembler loop-eligible). The
labels won, and the precheck's selection was taken as authoritative.

The work: `whats-to-test.sh` now groups every change into "In the app" /
"Behind the scenes" (nothing filtered), consuming the conventional-commit
prefix as the grouping signal and dropping it plus PR numbers from the
visible text. Type alone cannot group correctly — `feat(loop)` is the
counterexample — so behaviour-changing types defer to a tooling-scope list;
applied to the issue's build-210 example the rule reproduces the owner's own
2-of-10 classification exactly. PR-body first-sentence extraction rides on
the origin remote (fixtures have none, so the 25 existing hermetic cases
never touch the network) and falls back to the subject on any failure. The
heading keeps the build number: TestFlight shows it to the tester, and it
anchors "worked in 207" reports. Five new cases (26–30); break/restore
proved both the grouping case and the fallback case discriminate. Existing
cases updated only where they asserted the old visible format.

CodeRabbit: `Review rate limited` from the very first poll, terminal — the
third trial in this record set with `coderabbit_findings_first` a hole
(after 2026-08-12T024924Z-issue-64 and 2026-08-13T000119Z-issue-40). The
prediction in issue-40's record that this column "will be mostly holes" is
holding. CI resolved independently; wait cost 388s of the separate 15-minute
cap, fix phase skipped with nothing to act on.

Environment stumble, known shape: 1Password's SSH agent refused signing for
push/fetch mid-run (`communication with agent failed`), twice, surviving one
retry each time. The documented HTTPS-via-gh fallback with the
blank-then-set credential.helper worked on the first try both times. Commit
signing with the loop's own key was unaffected — only transport broke.

For a human in six weeks: the release-time PR-body fetch runs anonymous
until `testflight.yml` grants `pull-requests: read` and exports a token to
the notes step (owner follow-up in PR #101's body — workflows are off-limits
to the loop). ci.yml's smoke comment saying --print is "no network" is now
"no network required"; same follow-up note.
