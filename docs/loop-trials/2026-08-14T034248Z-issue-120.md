---
run_id: 2026-08-14T034248Z-issue-120
timestamp: 2026-08-14T03:42:48Z
prompt_sha: 362768d
issue: 120
kind: documentation
size: size:xs
outcome: pr-opened
wall_clock_seconds: 505
verification_result: pass
ci_result: pass
coderabbit_findings_first: unavailable
coderabbit_findings_after: none
test_proof_first: n/a
test_proof_domains: none
fix_rounds: 0
pr: 135
learning_added: none
---

Issue #120: the README said what WADdle *is* and never why it exists.
Reproduced cleanly at `83c60fd` — `grep -in 'predecessor\|successor\|parity'
README.md` returned nothing, exit 1, exactly as the issue measured on
2026-08-13 at `e02933b`.

The change is one new README section placed directly under the intro, before
"Working in this repository": it names the predecessors (the per-game
id/Tom Kidd DOOM-iOS-lineage apps — DOOM, DOOM II, Final DOOM, SIGIL), lists
the six predecessor behaviors as the parity baseline, explains why the
baseline changes scope calls using the founding spec's deferral of
touch-layout customization as the worked example, and links #111–#119 in a
table. Verification passes: the grep now returns six lines.
`Scripts/check-substrate.sh` passes. PR #135, `Closes #120`.

Two things a reader in six weeks would want to know.

**CodeRabbit never reviewed this PR.** Its row in `gh pr checks` came back
`Review rate limited` on the very first poll — 4.1's terminal non-review
state — so `coderabbit_findings_first` is `unavailable`, not `none`, and the
whole fix phase was skipped by the protocol rather than by choice. Worth
noting that the row's *bucket* is `pass` and its state `SUCCESS`; only the
description carries the rate-limit signal. Anything that reads the bucket
alone would conclude CodeRabbit approved the change. CI was polled on its
own from there and concluded `pass` at 330s into the wait, well inside the
900-second cap.

**`test_proof_first: n/a` is the honest verdict here, not a defect.** This is
a docs-only diff touching one file; reverting a README edit cannot make a
Swift or shell test fail, so the red-green harness has nothing to measure and
says so. Per section 4.1 I recorded it and did not act on it.

One judgment call worth flagging for the owner: the issue's verification
section also says "`mise run test` must still pass unchanged." I did not run
it. This worktree has no built engine, so a local run means a full bootstrap
(~25 min of the 45-minute work budget) to prove that a change to README.md
does not affect a Swift build. CI's build + unit tests on the PR cover that
claim directly and came back green. The PR body states this explicitly rather
than implying the local command was run.

The parity table is labelled as a filing record rather than a live checklist,
deliberately: #117 and #119 are already closed, and a README table that
tracks issue state would rot within the week. Entries are in ascending issue
order — an earlier draft grouped behavior gaps ahead of docs gaps, which
reads as a mistake without a header explaining the grouping.

Work budget used was ~175 seconds excluding the 330-second CI wait; the
run was gated by CI, not by the work.
