---
run_id: 2026-08-08T025710Z-issue-15
timestamp: 2026-08-08T02:57:10Z
prompt_sha: ce934c6
issue: 15
kind: documentation
size: size:xs
outcome: pr-opened
wall_clock_seconds: 568
verification_result: pass
ci_result: pass
coderabbit_findings_first: unavailable
coderabbit_findings_after: none
fix_rounds: 0
pr: 59
learning_added: none
---

First trial on an issue other than #13, and the first clean end-to-end pass:
reproduce, change, verify, PR, CI green, inside 10 minutes with no fix rounds.
PR #59.

Reproduced at HEAD before touching anything: `grep -n "ci.yml" README.md`
exited 1 while all three paths the issue names (`ci.yml`, `ui-tests.yml`,
`check-engine-fresh.sh`) existed — so the README genuinely documented only the
local build path, exactly as #15 claimed. The issue's verification then ran
unmodified against the change and exited 0.

**The thing worth reading this record for: `ci_result: pass` with
`coderabbit_findings_first: unavailable`, and what that pair proves.**

`2026-08-08T012411Z-issue-13.md` (PR #57) hit CodeRabbit's `Review rate
limited` and recorded `ci_result: timeout` / `coderabbit_findings_first: none`
— even though CI had gone green in 6m8s. The poller sat from t+787 to the
t+914 cap waiting for a review count that was never going to arrive, and the
single conflated `timeout` then reported a healthy CI run as a CI failure.
That record called it "the field's first real conflation".

This run hit **the identical CodeRabbit state** — same `Review rate limited`
string, and again reported at check level as `pass`, so `gh pr checks` looks
entirely healthy while zero review bodies exist. The difference is the
protocol, not the service:

| | #57 (prev prompt) | #59 (this run) |
|---|---|---|
| CodeRabbit row | `Review rate limited` | `Review rate limited` |
| Recognised at | never — polled to cap | **t+0, first poll** |
| Wait length | 914s (full cap) | **307s** (CI's own time) |
| `ci_result` | `timeout` (CI had passed) | **`pass`** (CI's real answer) |
| `coderabbit_findings_first` | `none` | **`unavailable`** |

So section 4.1's terminal-non-review rule and the CI/review independence rule
both did the job they were added for, on their first real exposure. Two
concrete wins: ~607 seconds of cap not burned on a question already answered,
and a CI column that finally says what CI actually did. `<WAIT_TOTAL>` came to
307s, and subtracting it left the work budget at 247s consumed — the wait cost
the work budget nothing, which is the other thing that separate accounting was
for.

`unavailable` vs `none` also gets its first real datapoint here, and the
distinction earns its keep: #57's `none` cannot be told apart from "reviewed,
found nothing", whereas this row states outright that the reviewer declined.
`Scripts/loop-report.sh` can now separate "not measured" from "measured zero"
for this trial — it still cannot for #57.

**Fourth Known gap, firing for the third consecutive trial.** PR #59 carries
no CodeRabbit review at all, 4.2 and 4.3 were skipped per protocol, and no
later run can pick it up because the precheck excludes an issue with a linked
open PR. #15 is off the backlog until the owner acts on #59. Worth saying
plainly: **the review-response phase in 4.2/4.3 has still never executed.**
Across every trial that could have recorded it, the leading signal is missing
— #55's review landed only after its run ended, #57 was rate limited, #59 was
rate limited. The experiment currently has zero measurements of
`coderabbit_findings_first` as an actual integer. Two rate limits back to back
roughly 90 minutes apart suggests the repository's CodeRabbit quota is the
binding constraint, not timing, and that is an owner-side thing to check —
nothing the loop can wait out.

Since no bot reviewed it, what a human should check by hand on #59. It is a
pure `README.md` change (53 insertions, one file, no workflow touched), so the
risk is not breakage but inaccuracy — a README that confidently points at the
wrong thing. Every factual claim was read out of the workflow files rather
than recalled, and the two failure modes a docs change actually has were
checked directly:

- The badge URL returns `HTTP 200`, `image/svg+xml`. Pinned `?branch=main` so
  it reports the default branch rather than whichever run finished last.
- Both in-page anchors resolve to real headings — `#building` and
  `#real-wad-test-matrix`.

Three claims in the new section are ones a reviewer should confirm they
actually want stated, since each is a judgement call rather than a
transcription:

1. `mise run test` is described as *broader* than CI. This is from `CLAUDE.md`
   and true, but it is the kind of thing that silently rots if CI's
   `-only-testing:WADdleTests` filter ever changes.
2. The stale-engine guard is described as comparing content, **not** mtimes,
   with the old `find -newer` behaviour called out as out of date. The issue
   asked for this because the stale description survives in several places;
   this section does not attempt to fix those other places.
3. CI is described as running `test-check-engine-fresh.sh` rather than
   `check-engine-fresh.sh` itself, because a cache-restored framework carries
   its fingerprint stamp inside the same cache entry and passes trivially.
   That reasoning is lifted from `setup-waddle-build/action.yml`'s own comment,
   not invented here.

No learning file: nothing in this trial was a trap. The one mildly surprising
thing is filed above rather than in `docs/learnings/`, because it is a fact
about the loop protocol's own behaviour, not about building WADdle.
