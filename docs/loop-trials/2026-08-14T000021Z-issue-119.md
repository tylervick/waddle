---
run_id: 2026-08-14T000021Z-issue-119
timestamp: 2026-08-14T00:00:21Z
prompt_sha: 362768d
issue: 119
kind: documentation
size: size:xs
outcome: pr-opened
wall_clock_seconds: 455
verification_result: pass
ci_result: pass
coderabbit_findings_first: unavailable
coderabbit_findings_after: none
test_proof_first: n/a
test_proof_domains: none
fix_rounds: 0
pr: 129
learning_added: none
---

Straightforward doc/code drift, reproduced exactly as issue #119 described it.
`README.md:203` claimed the stick dead zone ranged 0.05–0.4 with a 0.2 default
and `docs/manual-testing.md:37` told testers Reset to Defaults restores
1.00 / 0.20 / 1.00, while `TouchTuning.swift` has `stickDeadZoneRange =
0.0...0.4` and a default of `0.0`. Two edits, both numbers-only in substance.

Two things were worth the extra minute of checking. First, `ControlFeelView`
binds the slider straight to `TouchTuning.stickDeadZoneRange` rather than
repeating the bounds, so the README's range claim had exactly one source of
truth to match — no third value hiding in the UI layer. Second, grepping the
whole of both docs for dead-zone mentions found only those two concrete
numbers; the on-device tuning procedure lower in `manual-testing.md` uses
`<x.xx>` placeholders throughout, so it needed nothing.

One judgement call beyond the literal definition of done: the README entry now
says the value is *extra* dead zone layered on the 15% inner dead zone Woof!
already applies engine-side. Changing "default 0.2" to "default 0.0" while
leaving the prose reading "fraction of the stick's radius that ignores small
wobbles" would have swapped one wrong impression (wrong number) for another
(that wobble rejection is off by default, which is false — the engine's own
zone is still there). The reasoning is already recorded at
`TouchTuning.swift:25-30`; the README just never picked it up. Flagged in the
pull request, not silently done.

The same imprecise sentence still sits in the app itself, at
`ControlFeelView.swift:30`'s section footer. Deliberately left alone: #119 is
scoped docs-only and its own verification note says so, and touching app
source would have pulled Swift into a documentation change. Noted in the pull
request body as a follow-up rather than folded in.

`test_proof_first: n/a` is the honest reading here, not a gap — CI computed it
and said so. Nothing testable changed, so there was no red-green transition to
prove; a docs-only diff cannot produce one.

CodeRabbit never reviewed: its check row read `Review rate limited` 47 seconds
into the wait, which the protocol treats as a terminal answer rather than a
pending state, so `coderabbit_findings_first` is `unavailable` rather than a
count. CI was unaffected and got its own real answer — `Build + unit tests`
passed in 4m31s, 287 seconds into the wait. Per section 4.1 that combination
skips the fix phase entirely, so this pull request goes to the owner with no
review pass over it; per the fourth known gap, no later run will pick it up.
It is two changed numbers and one clarifying clause in prose, so that is a
small exposure here, but it is real and worth stating rather than leaving the
clean `ci_result: pass` to imply the change was scrutinised more than it was.

Timing: 455s wall clock, of which 288s was the CI wait, leaving 167s of the
45-minute work budget consumed.
