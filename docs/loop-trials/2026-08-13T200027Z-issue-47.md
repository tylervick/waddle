---
run_id: 2026-08-13T200027Z-issue-47
timestamp: 2026-08-13T20:00:27Z
prompt_sha: 362768d
issue: 47
kind: bug
size: size:s
outcome: pr-opened
wall_clock_seconds: 1632
verification_result: pass
ci_result: timeout
coderabbit_findings_first: unavailable
coderabbit_findings_after: none
test_proof_first: error
test_proof_domains: none
fix_rounds: 0
pr: 110
learning_added: none
---

Reproduced exactly as issue 47 described, first statically (the counter is
incremented in four shim entry points, reset nowhere — the existing unwind
reset block covers only the other touch statics), then at runtime: the new
two-session UI test, run at unfixed HEAD, watched the second session — which
performed zero touches — report the first session's count of 4. The fix is
one line at session start in `WoofIOS_Run`, next to `I_ResetErrorMessages()`,
exactly where the issue pointed; the issue's own warning about the unwind
branch is upheld by the two pre-existing `count > 0` tests, which still pass.
The issue's verification command, run unmodified, succeeded
(`** TEST SUCCEEDED **`; `testSessionSurvivesRotation` skipped via its own
pre-existing environmental guard on this simulator — unrelated).

Environment cost, same as the issue-46 trial: the fresh Orca worktree
inherited a `Vendor/build` whose CMake caches hardcode the owner's primary
checkout path (trap 2 of `docs/learnings/worktree-setup-traps.md`, already
documented, so no learning owed). `rm -rf Vendor/build` plus a full ~96s
rebuild, recurring every run until worktree creation stops copying it.

The real surprise: **CI never started.** Not slow — absent. `gh run list
--branch <branch>` returned `[]` for the full 924-second wait and again after
it; the only check row on PR 110 the whole time was CodeRabbit's. `ci.yml` is
`active` and triggers on `pull_request`, and same-day earlier trials (#45,
#46) got real CI runs, so this looks like a GitHub Actions-side failure to
create the run, not a repo config change. Per protocol the cap expired with
CI unconcluded: `ci_result: timeout`, and the red-green proof was never
computed (`test_proof_first: error`, `test_proof_domains: none`).

CodeRabbit was rate-limited again — its check row read `Review rate limited`
at t=12s, the second consecutive trial, so `coderabbit_findings_first` is
`unavailable` (not `none`) and 4.2/4.3 were skipped by protocol. Per Known
gaps, PR 110 therefore leaves section 4 with neither a review nor a CI
verdict, and no later run will revisit it — it is the owner's from here. The
work itself is in better shape than those fields suggest: local verification
passed in full, with a genuine red observed before the fix.
