<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- CI-only. App/ExportOptions.plist stays automatic for local use.
         Manual signing here is a safety property, not a preference: with
         automatic signing plus -allowProvisioningUpdates, a failure to find
         the identity makes xcodebuild MINT a new Apple Distribution
         certificate whose key dies with the runner. Apple caps those at 2-3
         and the only cleanup is manual revocation in the portal. -->
    <key>method</key><string>app-store-connect</string>
    <key>teamID</key><string>352UZEKYPP</string>
    <key>signingStyle</key><string>manual</string>
    <key>signingCertificate</key><string>Apple Distribution</string>
    <key>provisioningProfiles</key>
    <dict>
        <!-- Must match App/project.yml's Release PROVISIONING_PROFILE_SPECIFIER
             exactly — the archive and the export resolve the profile
             independently, so a name that drifts here fails at export with
             'No "iOS App Store" profiles ... matching <name> are installed'
             AFTER a successful archive.

             Manually managed on purpose. An Xcode-managed profile cannot be
             used under manual signing, which is why this is not the
             "iOS Team Store Provisioning Profile" Xcode maintains.

             Installed on the runner from the PROVISIONING_PROFILE_BASE64
             secret. Expires 2027-05-02, the same day as the certificate it is
             bound to; regenerate both before then. The failure mode on expiry
             is an opaque signing error. -->
        <key>com.tylervick.waddle</key>
        <string>WADdle App Store CI</string>
    </dict>
    <key>uploadSymbols</key><true/>
</dict>
</plist>
